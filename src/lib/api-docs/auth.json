{
	"title": "The Auth object",
	"endPoints": [
		{
			"id": "login",
			"method": "POST",
			"route": "api/auth/login",
			"description": "Authenticate a user and return a JWT token.",
			"bodyParameters": [
				{
					"name": "username",
					"type": "string",
					"description": "The user's username.",
					"example": "johndoe"
				},
				{
					"name": "password",
					"type": "string",
					"description": "The user's password.",
					"example": "securepassword123"
				}
			],
			"statusCodes": [
				{
					"code": 200,
					"description": "Successful authentication. Returns a JWT token."
				},
				{
					"code": 400,
					"description": "Bad request. Missing or invalid parameters."
				},
				{
					"code": 401,
					"description": "Unauthorized. Invalid username or password."
				},
				{
					"code": 500,
					"description": "Internal server error."
				}
			]
		},
		{
			"id": "register",
			"method": "POST",
			"route": "api/auth/register",
			"description": "Register a new user.",
			"bodyParameters": [
				{
					"name": "username",
					"type": "string",
					"description": "The desired username for the new user.",
					"example": "timapple"
				},
				{
					"name": "email",
					"type": "string",
					"description": "The email address of the new user.",
					"example": "timapple@uw.edu"
				}
			]
		}
	],
	"attributes": [
		{
			"name": "token",
			"type": "string",
			"description": "A JWT token used for authenticating subsequent requests.",
			"example": ""
		},
		{
			"name": "username",
			"type": "string",
			"description": "The user's username.",
			"example": "johndoe"
		},
		{
			"name": "email",
			"type": "string",
			"description": "The user's email address. Must be a valid email format with uw.edu domain.",
			"example": "johndoe@uw.edu"
		}
	]
}
